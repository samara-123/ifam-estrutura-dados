# ifam-estrutura-dados
samara
echo "# ifam-estrutura-dados" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/samara-123/ifam-estrutura-dados.git
git push -u origin main